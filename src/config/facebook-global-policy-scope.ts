/**
 * Facebook 全局运营策略与冷启动完成事实的**行作用域键**（change unify-facebook-global-policy-across-targets）。
 *
 * 这三张表此前按 `execution_target` 分行，dev 一行、ol 一行，每个进程只读自己那行。后台按主机各部署
 * 一套且页面不标注目标，运营以为在改一份全局配置、实际只改了自己连的那一边——已经造成一次线上事故
 * （dev 改成 5 天、OL 仍 7 天，OL 上第 6/7 天的账号一直显示未完成且配额继续被冷启动曲线夹住，
 * 两边各自都在如实汇报自己那份配置算出来的结果，所以没有任何一处报错）。
 *
 * 现在这些行只有一份，用本常量作行键。
 *
 * ## 三条使用纪律
 *
 * 1. **只有一份定义。** 三个属主文件（策略存储 / 群评论策略存储 / 客户环境存储）与同步读快照源都
 *    从这里取。各写一份字符串字面量的现形方式不是报错，而是某一处仍去读那两行旧数据——而旧行在
 *    合并那一刻起就冻结了，读到的是合并当时的值，且逐格看起来都很正常。
 * 2. **它不是部署目标，MUST NOT 与 `AIDCP_DEPLOY_ENV` 混用。** 进程自己的目标仍然决定别的事
 *    （持久任务隔离、schema 要求集、`cadence_source`）；本常量只决定"读哪一行配置"。
 * 3. **它是中间态，不是终点。** 终点是把 `execution_target` 这一列从三张主表上删掉；那属收缩类
 *    DDL，按 `migrations/README.md` 必须独立 change、独立部署。在那一段落地之前，本常量是
 *    "分行键还在、但只用一个值"的过渡形态。**MUST NOT 因为"现在能跑"就让它长期驻留**——
 *    一个还留着的分行维度迟早会被重新用起来。
 */
export const FACEBOOK_GLOBAL_POLICY_SCOPE = 'all';
